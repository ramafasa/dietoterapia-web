---
/**
 * Invalid Invitation Error Page
 * Route: /auth/invitation-invalid?reason=missing|expired|not_found
 *
 * Displayed when invitation token is missing, invalid, expired, or already used
 */

import Layout from '@/layouts/Layout.astro'
import Alert from '@/components/Alert'

export const prerender = false

// Get reason from query string
const reason = Astro.url.searchParams.get('reason') || 'missing'

// SEO metadata
const title = 'Nieprawidłowe zaproszenie | Dietoterapia - Paulina Maciak'
const description = 'Link zaproszenia jest nieprawidłowy lub wygasł'
---

<Layout title={title} description={description}>
  <section class="min-h-screen bg-neutral-light py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-2xl mx-auto">
      <!-- Header -->
      <div class="text-center mb-8">
        <h1 class="text-3xl font-heading font-bold text-neutral-dark mb-2">
          Nieprawidłowe zaproszenie
        </h1>
      </div>

      <!-- Error content -->
      <div class="bg-white rounded-lg shadow-md p-6 sm:p-8">
        {reason === 'missing' && (
          <Alert
            client:load
            variant="error"
            message="Link zaproszenia jest nieprawidłowy. Upewnij się, że skopiowałeś pełny adres URL z wiadomości e-mail."
          />
        )}

        {reason === 'expired' && (
          <Alert
            client:load
            variant="warning"
            message="Link zaproszenia wygasł lub został już wykorzystany. Poproś dietetyka o nowe zaproszenie."
          />
        )}

        {reason === 'not_found' && (
          <Alert
            client:load
            variant="error"
            message="Link zaproszenia nie został znaleziony w systemie. Sprawdź, czy adres URL jest poprawny."
          />
        )}

        <!-- Help section -->
        <div class="mt-8 space-y-4">
          <h2 class="text-lg font-heading font-semibold text-neutral-dark">
            Co mogę zrobić?
          </h2>

          <ul class="space-y-3 text-neutral-dark/80">
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-primary flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
              <span>Sprawdź wiadomość e-mail z zaproszeniem i skopiuj pełny link</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-primary flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
              <span>Upewnij się, że link nie został jeszcze wykorzystany do rejestracji</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-primary flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
              <span>Jeśli link wygasł, poproś dietetyka o wysłanie nowego zaproszenia</span>
            </li>
          </ul>

          <!-- CTA buttons -->
          <div class="mt-8 flex flex-col sm:flex-row gap-4">
            <a
              href="/kontakt"
              class="flex-1 text-center bg-primary text-white font-semibold py-3 px-6 rounded-lg hover:bg-primary/90 focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-colors"
            >
              Skontaktuj się z dietetykiem
            </a>
            <a
              href="/"
              class="flex-1 text-center bg-gray-100 text-neutral-dark font-semibold py-3 px-6 rounded-lg hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-gray-300 focus:ring-offset-2 transition-colors"
            >
              Strona główna
            </a>
          </div>

          <!-- Already have account -->
          <p class="text-sm text-gray-600 text-center mt-6">
            Masz już konto?{' '}
            <a href="/auth/login" class="text-primary font-medium hover:underline">
              Zaloguj się
            </a>
          </p>
        </div>
      </div>
    </div>
  </section>
</Layout>

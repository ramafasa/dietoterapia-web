---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ToastProvider from '../components/ToastProvider';
import GoogleAnalytics from '../components/GoogleAnalytics.astro';
import MetaPixel from '../components/MetaPixel.astro';
import CookieBanner from '../components/CookieBanner';
import LocalBusinessSchema from '../components/LocalBusinessSchema.astro';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  includeRecaptcha?: boolean;
}

const {
  title = 'Dietetyk Brzeziny, Koluszki, Andrespol, Łódź | Paulina Maciak - Dietoterapia',
  description = 'Dietetyk kliniczny Paulina Maciak. Konsultacje online i stacjonarne w Brzezinach, Koluszkach, Andrespolu. Specjalizacja: insulinooporność, PCOS, tarczyca, redukcja wagi.',
  ogImage = '/images/logo.png',
  includeRecaptcha = false,
} = Astro.props;

// Canonical URL - prevent duplicate content
const canonicalURL = new URL(Astro.url.pathname, Astro.site);

// reCAPTCHA Site Key (public) - loaded only when includeRecaptcha === true
const recaptchaSiteKey = import.meta.env.PUBLIC_RECAPTCHA_SITE_KEY;
---

<!doctype html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <!-- SEO Meta Tags -->
    <title>{title}</title>
    <meta name="description" content={description} />

    <!-- Canonical URL -->
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(ogImage, Astro.site)} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="pl_PL" />
    <meta property="og:site_name" content="Dietoterapia - Paulina Maciak" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={new URL(ogImage, Astro.site)} />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/logo.png" />

    <!-- Preconnect for Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- Google Fonts: Montserrat (600, 700) i Open Sans (400, 600) -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet" />

    <!-- Google Analytics 4 (conditional loading based on cookie consent) -->
    <GoogleAnalytics />

    <!-- Meta Pixel (Facebook Pixel) - conditional loading based on cookie consent -->
    <MetaPixel />

    <!-- Structured Data: LocalBusiness Schema -->
    <LocalBusinessSchema />

    <!-- Google reCAPTCHA v3 (loaded only on pages with forms) -->
    {includeRecaptcha && recaptchaSiteKey && (
      <script
        src={`https://www.google.com/recaptcha/api.js?render=${recaptchaSiteKey}`}
        async
        defer
      ></script>
    )}
  </head>
  <body>
    <ToastProvider client:load />
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
    <CookieBanner client:load />
  </body>
</html>

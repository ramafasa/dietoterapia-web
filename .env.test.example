# Test Environment Variables
# Copy this file to .env.test for local testing
# These values are used by Vitest and Playwright tests

# Environment
NODE_ENV=test

# Database (Testcontainers will override this with actual connection string)
# This is just a placeholder for reference
DATABASE_URL=postgresql://test:test@localhost:5432/test_db

# Lucia Auth Session Secret (must be at least 32 characters)
LUCIA_SESSION_SECRET=test-session-secret-key-for-testing-only-min-32-chars

# Application URL
SITE_URL=http://localhost:4321

# SMTP (not used in tests, but required by some modules)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
CONTACT_EMAIL=test@example.com

# Test-specific variables (set by global setup)
# These are populated dynamically during test execution
# TEST_PATIENT_EMAIL=
# TEST_PATIENT_PASSWORD=
# TEST_DIETITIAN_EMAIL=
# TEST_DIETITIAN_PASSWORD=
# TEST_DATABASE_URL=
